package io.muserver;

/** The supported HAProxy PROXY protocol wire formats. */
public enum HAProxyProtocolVersion {
    /** The text-based PROXY protocol version 1. */
    V1,
    /** The binary PROXY protocol version 2. */
    V2
}
